# PostgreSQL
POSTGRES_USER=aml_user
POSTGRES_PASSWORD=changeme
POSTGRES_DB=aml_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Kafka
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# OpenAI (Phase 4 — gpt-4o-mini for tool selection and evaluation)
OPENAI_API_KEY=
